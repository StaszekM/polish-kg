# Polish language Knowledge Graph Resources
Master thesis in progress 🚧

# How to run
Just run `docker compose up`. This will install APT packages and Python dependencies. You may then access the shell and do other stuff, too.

# Required packages in environment (if running by local)
- graphviz
- graphviz-dev
- gcc
- python3-lxml
